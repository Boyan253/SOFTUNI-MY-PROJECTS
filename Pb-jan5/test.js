function solve(input) {

  let text = (input[0]);
  let textletter = text.charAt(1)
  console.log(textletter)

  for(let i = 0; i < text.length; i++) {
    console.log(text[i])
  }
}
solve(["boqn"])