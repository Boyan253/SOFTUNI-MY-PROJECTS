function workingHours(input) {
const hours = (input[0])
const day = (input[1])

switch (day) {
    case "Monday":
    case "Tuesday":
    case "Wednesday":
    case "Thursday" :
    case "Friday":
    case "Saturday":
        
        break;
        default: console.log("closed");
        
        if (hours > 10 && hours < 18)
        {
            switch (day) {
            case "Monday":
                case "Tuesday":
                case "Wednesday":
                case "Thursday" :
                case "Friday":
                case "Saturday":
            }
            console.log("open"); }

       
        
           
        
        }}


